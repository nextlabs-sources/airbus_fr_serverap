<!-- build_compile.xml

Description:
	This ANT script publishes installers and build artifacts to s:/build.
	
	To use this script, you must update properties is <project root>/scripts/build.properties or overide 
	them from commandline (using -D option). For example:
		
		set PATH=C:\depot\external\apache-ant\apache-ant-1.8.4\bin;%PATH%
		ant -XX:MaxPermSize=512m -Dnlbuildroot=C:/depot/PolicyStudio/dev/BuildScript_1205 -Dexternal.dir=c:/depot/external -Dnlexternaldir=c:/depot/external -Dbuild_number=10001 -f scripts/build_publish.xml
		
	or
		
		set PATH=C:\depot\external\apache-ant\apache-ant-1.8.4\bin;%PATH%
		set ANT_OPTS=-XX:MaxPermSize=512m -Dnlbuildroot=%NLBUILDROOT% -Dexternal.dir=%NLEXTERNALDIR% -Dnlexternaldir=%NLEXTERNALDIR% -Dbuild_number=10001 
		ant -f scripts/build_publish.xml -v	
-->
<project name="build_compile" default="all" basedir=".">

	<!-- Propery files -->
	<property file="${basedir}/build.properties" />
	<property environment="env" />

	<!-- Global properties -->
	<property name="xlib_jar_dir" location="${nlbuildroot}/xlib/jar" />
	<property name="build_dir" location="${nlbuildroot}/build" />
	<property name="classes_dir" value="${build_dir}/classes" />
	<property name="package_dir" value="${build_dir}/${package_name}" />

	<property name="dist_zip_file_name" value="${package_name}-${version_str}-${build_number}-${version_build_short}-${build_date_long}.zip" />

	<!-- Dump properties -->
	<echoproperties />

	<echo message="INFO: Dump properties completed" />

	<!-- Sanity checks -->
	<fail message="nlbuildroot is not set" unless="nlbuildroot" />
	<fail message="nlbuildroot is empty">
		<condition>
			<equals arg1="${nlbuildroot}" arg2="" trim="true" />
		</condition>
	</fail>
	<available file="${nlbuildroot}" type="dir" property="local.nlbuildroot_exists" />
	<fail message="nlbuildroot directory does not exist - ${nlbuildroot}" unless="local.nlbuildroot_exists" />

	<echo message="INFO: Sanity check completed (no error)" />

	<path id="compile.classpath">
		<fileset dir="${nlbuildroot}/lib/jar">
			<include name="*.jar"/>
		</fileset>
		<fileset dir="${xlib_jar_dir}">
			<include name="*.jar"/>
		</fileset>
	</path>

	<!-- Global tasks -->
	<tstamp>
		<format property="local.now" pattern="EEE, d MMM yyyy HH:mm:ss Z" />
	</tstamp>

	<echo message="version_str = ${version_str}" />
	<echo message="time = ${local.now}" />

	<!-- Targets -->
	<target name="all" description="Default for all" depends="clean, compile, package" />

	<target name="clean" description="Delete all build artifacts" depends="">
		<delete includeemptydirs="true" dir="${build_dir}"/>
	</target>

	<target name="compile" description="Compile the project" depends="clean" >

		<mkdir dir="${classes_dir}" />

		<javac srcdir="${nlbuildroot}/src" destdir="${classes_dir}" debug="true">
			<classpath refid="compile.classpath"/>
			<include name="**/*.java" />
		</javac>

		<jar destfile="${build_dir}/UserReferentialPlugin.jar">
			<fileset dir="${classes_dir}" excludes="**/ResourceAttributeProvider.class"/>
			<manifest>
				<attribute name="Java-Version" value="${java.version}"/>
				<attribute name="Implementation-Vendor" value="NextLabs, Inc."/>
				<attribute name="Implementation-Title" value="${product_name}"/>
				<attribute name="Implementation-Version" value="${version_str} (${build_number})"/>
				<attribute name="Implementation-Time" value="${local.now}"/>
				<attribute name="Provider-Class" value="com.nextlabs.provider.UserAttributeProvider"/>
			</manifest>
		</jar>

		<jar destfile="${build_dir}/ResourceReferentialPlugin.jar">
			<fileset dir="${classes_dir}" excludes="**/UserAttributeProvider.class"/>
			<manifest>
				<attribute name="Java-Version" value="${java.version}"/>
				<attribute name="Implementation-Vendor" value="NextLabs, Inc."/>
				<attribute name="Implementation-Title" value="${product_name}"/>
				<attribute name="Implementation-Version" value="${version_str} (${build_number})"/>
				<attribute name="Implementation-Time" value="${local.now}"/>
				<attribute name="Provider-Class" value="com.nextlabs.provider.ResourceAttributeProvider"/>
			</manifest>
		</jar>
		
		<javadoc destdir="${nlbuildroot}/doc/api"
		         author="true"
		         version="true"
		         use="true"
		         windowtitle="Referencial Plugin API">
		  <classpath refid="compile.classpath"/>
			
		  <fileset dir="${nlbuildroot}/src" defaultexcludes="yes">
		  </fileset>

		  <doctitle><![CDATA[<h1>Referencial plugin</h1>]]></doctitle>
		  <bottom><![CDATA[<i>Copyright &#169; 2019 Airbus Helicopters, Inc. All Rights Reserved.</i>]]></bottom>
		  <tag name="todo" scope="all" description="To do:"/>
		  <group title="Group 1 Packages" packages="com.nextlabs.cache.*"/>
		  <link offline="true" href="https://docs.oracle.com/javase/8/docs/api/" packagelistLoc="C:\tmp"/>
		  <link href="https://docs.oracle.com/javase/8/docs/api/"/>
		</javadoc>


	</target>

	<target name="package" description="builds each plugin module">
		<mkdir dir="${package_dir}" />

		<copy todir="${package_dir}/jars" overwrite="true">
			<fileset file="${build_dir}/*.jar" />
		</copy>

		<copy todir="${package_dir}/doc" overwrite="true">
			<fileset file="${nlbuildroot}/doc/**" />
		</copy>

		<copy todir="${package_dir}/config" overwrite="true">
			<fileset dir="${nlbuildroot}/etc/" />
		</copy>

		<copy todir="${package_dir}/xlib" overwrite="true">
			<fileset dir="${xlib_jar_dir}" />
		</copy>

		<zip destfile="${build_dir}/${dist_zip_file_name}" encoding="UTF-8">
			<fileset dir="${build_dir}/${package_name}/">
				<include name="**/*.*" />
			</fileset>
		</zip>

	</target>

</project>

